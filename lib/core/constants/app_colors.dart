import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary — Soft Blue ───────────────────────────────────────────────────
  static const Color primary          = Color(0xFF5B8FD4);
  static const Color primaryLight     = Color(0xFFEEF3FB);
  static const Color primaryDark      = Color(0xFF185FA5);
  static const Color primaryText      = Color(0xFF0C447C);

  // ── Secondary — Sage Green ────────────────────────────────────────────────
  static const Color secondary        = Color(0xFF5A9E4A);
  static const Color secondaryLight   = Color(0xFFEDF5E8);
  static const Color secondaryDark    = Color(0xFF3B6D11);
  static const Color secondaryText    = Color(0xFF27500A);

  // ── Accent — Warm Lavender ────────────────────────────────────────────────
  static const Color accent           = Color(0xFF8B72D4);
  static const Color accentLight      = Color(0xFFF0ECFB);
  static const Color accentDark       = Color(0xFF534AB7);
  static const Color accentText       = Color(0xFF3C3489);

  // ── Neutral Background — Soft Off-White ──────────────────────────────────
  static const Color background       = Color(0xFFF9F8F6);
  static const Color surface          = Color(0xFFFFFFFF);
  static const Color surfaceSecondary = Color(0xFFF1EFE8);

  // ── Text — Deep Slate ─────────────────────────────────────────────────────
  static const Color textPrimary      = Color(0xFF1A1A1A);
  static const Color textSecondary    = Color(0xFF5F5E5A);
  static const Color textTertiary     = Color(0xFF888780);
  static const Color textHint         = Color(0xFFB4B2A9);

  // ── Semantic ──────────────────────────────────────────────────────────────
  static const Color success          = Color(0xFF5A9E4A);
  static const Color successLight     = Color(0xFFEDF5E8);
  static const Color warning          = Color(0xFFC4861A);
  static const Color warningLight     = Color(0xFFFDF3E3);
  static const Color error            = Color(0xFFD85A30);
  static const Color errorLight       = Color(0xFFFAECE7);
  static const Color info             = Color(0xFF5B8FD4);
  static const Color infoLight        = Color(0xFFEEF3FB);

  // ── Crisis / Emergency ────────────────────────────────────────────────────
  static const Color crisis           = Color(0xFFE24B4A);
  static const Color crisisLight      = Color(0xFFFCEBEB);
  static const Color crisisText       = Color(0xFF791F1F);

  // ── Borders ───────────────────────────────────────────────────────────────
  static const Color borderLight      = Color(0xFFE8E6E0);
  static const Color borderMedium     = Color(0xFFD3D1C7);
  static const Color borderDark       = Color(0xFFB4B2A9);

  // ── Mood Colors ───────────────────────────────────────────────────────────
  static const Color moodSad          = Color(0xFF85B7EB);
  static const Color moodStressed     = Color(0xFFEF9F27);
  static const Color moodNeutral      = Color(0xFF888780);
  static const Color moodCalm         = Color(0xFF5DCAA5);
  static const Color moodMotivated    = Color(0xFF5B8FD4);

  // ── Chart Colors ─────────────────────────────────────────────────────────
  static const Color chartMood        = Color(0xFF7BADD9);
  static const Color chartStress      = Color(0xFF7CBD6B);
  static const Color chartBurnout     = Color(0xFFD4537E);
}